TODO:

- animations when changing post status from posts list
- icons

fix image links again - and do the same for all links actually

- keep nav links (start with "#" probably)
- website title in the tab bar

back to top button

figure out centos go install, try 1.20 profile optimized compilation

have post creation return only the created post? what if state gets desynced in some cursed way? refreshing i guess

UI scaling for mobile: title sizes, typography prose-size setting

having signin and fetch posts as one request?

hook for logging out if loggenin cookie not set??

- loading thing when submitting a post ffs
- loading when saving a post

set up logging
